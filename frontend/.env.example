# URL of the deployed FastAPI backend (no trailing slash).
# Set this in Azure Static Web Apps application settings or as a GitHub Actions secret.
NEXT_PUBLIC_API_URL=https://your-backend.azurewebsites.net
